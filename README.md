# ATLAS AI (trading intelligence) lives at the repo root.

# Separately, the Motion X engagement bot lives in:
#   motion-bot/
#
#   cd motion-bot
#   pip install -r requirements.txt
#   cp .env.example .env
#   python main.py once --dry-run
